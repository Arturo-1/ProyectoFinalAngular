import { TheadTableDirective } from './thead-table.directive';

describe('TheadTableDirective', () => {
  it('should create an instance', () => {
    const directive = new TheadTableDirective();
    expect(directive).toBeTruthy();
  });
});
